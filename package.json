{
  "name": "plakotbot",
  "version": "1.0.0",
  "description": "bot for telegram, generate posters",
  "main": "app.js",
  "scripts": {
    "watch": "babel -w src/ -d dist/",
    "build": "babel src/ -d dist/",
    "serve": "babel -w src/ -d dist/ || pm2 start process.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "starter"
  ],
  "author": "ars33",
  "license": "ISC",
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-stage-0": "^6.16.0",
    "nodemon": "^1.11.0"
  },
  "dependencies": {
    "axios": "^0.16.2",
    "express": "^4.15.3"
  }
}
